name: ci

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
      - run: python -m pip install -e services/sidecar[dev] jsonschema
      - run: npm ci
        working-directory: packages/clawtune-plugin
      - run: python tools/validate_contracts.py
      - run: python -m swe_rebench.prepare
      - run: bash -n swe_rebench/.runtime/assets/setup.sh
      - run: bash -n swe_rebench/.runtime/assets/entrypoint.sh
      - run: cd services/sidecar && pytest
      - run: python -m pytest tests -q --basetemp .pytest-tmp-root
      - run: cd packages/clawtune-plugin && npm run typecheck && npm test && npm run build && npm pack
